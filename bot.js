# Octagon Telegram Bot Commands

Проект для задания по Telegram-боту на Node.js.

## Что делает бот

- `/start` — отправляет сообщение `Привет, октагон!`
- `/help` — показывает список команд `/site` и `/creator` с описанием
- `/site` — отправляет ссылку на сайт Октагона
- `/creator` — отправляет ФИО создателя бота

## Установка

```bash
npm install
```

## Настройка токена

Создайте файл `.env` рядом с `bot.js` и добавьте токен бота:

```env
BOT_TOKEN=ваш_токен_бота
```

Файл `.env` добавлен в `.gitignore`, поэтому токен не попадет в GitHub.

## Запуск

```bash
npm start
```

Для запуска с nodemon:

```bash
npm run dev
```

## Быстрый запуск через run.bat

Для запуска на Windows откройте файл `run.bat`. Он установит зависимости через `npm install` и выполнит `npm start`.

Токен Telegram-бота уже добавлен в локальный файл `.env`. Не публикуйте `.env` в GitHub.
